# Миграция бэкенда на Convex

## 📋 Обзор миграции

Полная миграция корпоративного портала Dorren с мок-данных на Convex базу данных.

### ✅ Что сделано

1. **Создана схема базы данных** в `convex/schema.ts`
2. **Реализованы API функции** для всех сущностей:
   - `convex/users.ts` - управление пользователями
   - `convex/news.ts` - управление новостями
   - `convex/tasks.ts` - управление задачами
   - `convex/docs.ts` - управление документами
   - `convex/courses.ts` - управление курсами
   - `convex/auth.ts` - система авторизации
3. **Обновлен клиент** в `convexClient.ts`
4. **Интегрированы компоненты** с Convex API
5. **Создан скрипт инициализации** `scripts/initConvex.ts`

## 🚀 Инструкции по настройке

### 1. Настройка Convex проекта

```bash
# Войти в аккаунт Convex (если не вошли)
npx convex login

# Запустить dev сервер Convex
npx convex dev

# Или использовать готовый скрипт
npm run convex:dev
```

### 2. Настройка переменных окружения

1. Скопируйте `.env.example` в `.env`
2. Получите URL вашего Convex проекта из dashboard или терминала
3. Добавьте URL в `.env`:

```env
VITE_CONVEX_URL=https://your-project-name.convex.cloud
```

### 3. Инициализация данных

```bash
# Убедитесь, что Convex dev сервер запущен
# Затем выполните инициализацию данных
npm run convex:init
```

Или напрямую через Node.js:
```bash
node scripts/initConvex.ts
```

### 4. Запуск приложения

```bash
# Обычный запуск (данные будут загружаться из Convex)
npm run dev
```

## 📊 Структура данных

### Таблицы Convex

1. **users** - Пользователи системы
2. **news** - Новости компании
3. **tasks** - Задачи (Kanban доска)
4. **docs** - Документы компании
5. **courses** - Обучающие курсы
6. **authCodes** - Коды авторизации
7. **sessions** - Пользовательские сессии

### Связи между таблицами

- `tasks.assigneeEmail` → `users.email`
- Авторизация через email/password (код подтверждения)
- Сессии с TTL 24 часа

## 🔧 API Endpoints

### Users
- `users.getUsers` - получить всех пользователей
- `users.getUserByEmail` - найти по email
- `users.updateUser` - обновить пользователя
- `users.initializeUsers` - массовая инициализация

### News
- `news.getNews` - получить все новости
- `news.getNewsById` - найти по ID
- `news.getNewsBySlug` - найти по slug
- `news.createNews` - создать новость
- `news.updateNews` - обновить новость
- `news.initializeNews` - массовая инициализация

### Tasks
- `tasks.getTasks` - получить все задачи
- `tasks.getTasksByStatus` - фильтр по статусу
- `tasks.getTasksByAssignee` - фильтр по исполнителю
- `tasks.createTask` - создать задачу
- `tasks.updateTask` - обновить задачу
- `tasks.initializeTasks` - массовая инициализация

### Docs
- `docs.getDocs` - получить все документы
- `docs.getDocById` - найти по ID
- `docs.getDocsByCategory` - фильтр по категории
- `docs.createDoc` - создать документ
- `docs.updateDoc` - обновить документ
- `docs.initializeDocs` - массовая инициализация

### Courses
- `courses.getCourses` - получить все курсы
- `courses.getCourseById` - найти по ID
- `courses.getCoursesByCategory` - фильтр по категории
- `courses.createCourse` - создать курс
- `courses.updateCourse` - обновить курс
- `courses.updateCourseProgress` - обновить прогресс
- `courses.initializeCourses` - массовая инициализация

### Auth
- `auth.requestCode` - запросить код авторизации
- `auth.verifyCode` - подтвердить код и создать сессию

## 🧪 Тестирование

### Проверка инициализации

```bash
# Проверить, что данные инициализированы
npx convex run "data:users"
npx convex run "data:news"
npx convex run "data:tasks"
```

### Проверка в браузере

1. Откройте приложение `http://localhost:3000`
2. Проверьте загрузку данных в каждом разделе:
   - Главная страница
   - Новости
   - Задачи (Kanban доска)
   - Обучение
   - Документы

### Тестирование авторизации

1. Нажмите кнопку "Войти" в правом верхнем углу
2. Введите email (например, `admin@dorren.com`)
3. Получите код подтверждения (будет выведен в консоль Convex)
4. Введите код и войдите в систему
5. Проверьте переключение в админ режим

## 🛠 Устранение неполадок

### Ошибки подключения

**Проблема**: `VITE_CONVEX_URL не установлен`
```bash
# Решение: Добавьте URL в .env файл
echo "VITE_CONVEX_URL=https://your-project.convex.cloud" >> .env
```

**Проблема**: Ошибки CORS
```bash
# Убедитесь, что Convex dev сервер запущен
npx convex dev
```

### Ошибки инициализации

**Проблема**: Данные не загружаются
```bash
# Проверьте статус Convex
npx convex dashboard

# Повторно запустите инициализацию
npm run convex:init
```

### Ошибки авторизации

**Проблема**: Коды не приходят
```bash
# Проверьте логи Convex
npx convex logs auth:requestCode
```

## 🔄 Миграция с старых данных

### Если есть существующие данные

1. Экспортируйте данные из старой системы
2. Преобразуйте в формат Convex
3. Выполните массовую загрузку

### Пример скрипта импорта

```typescript
// scripts/importLegacy.ts
import { ConvexHttpClient } from 'convex/browser';

const client = new ConvexHttpClient(process.env.VITE_CONVEX_URL!);

// Импорт из CSV/JSON файлов
const users = await loadLegacyUsers();
await client.mutation("users/initializeUsers", { users });
```

## 📈 Производительность

### Оптимизации Convex

1. **Индексы** - созданы для быстрого поиска
2. **Кэширование** - встроенное в Convex
3. **Реального времени** - автоматические обновления

### Мониторинг

```bash
# Просмотр производительности
npx convex dashboard

# Логи запросов
npx convex logs
```

## 🚀 Деплой в продакшн

### Подготовка

```bash
# Сборка приложения
npm run build

# Деплой Convex функции
npx convex deploy
```

### Переменные продакшн

```env
VITE_CONVEX_URL=https://your-production-project.convex.cloud
```

### CI/CD

```yaml
# .github/workflows/deploy.yml
name: Deploy
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: '18'
      - run: npm install
      - run: npm run build
      - run: npx convex deploy
        env:
          CONVEX_TOKEN: ${{ secrets.CONVEX_TOKEN }}
```

## 🔒 Безопасность

### Текущие меры

1. **Аутентификация** - код подтверждения на email
2. **Авторизация** - роли admin/user
3. **Сессии** - TTL 24 часа
4. **Валидация** - схемы данных

### Рекомендации

1. Добавить rate limiting
2. Логирование действий администратора
3. Резервное копирование данных
4. HTTPS в продакшн

## 📝 Заключение

Миграция на Convex обеспечивает:

✅ **Масштабируемость** - автоматическое масштабирование
✅ **Реального времени** - мгновенные обновления
✅ **Безопасность** - встроенная аутентификация
✅ **Простоту разработки** - TypeScript типы, автогенерация
✅ **Надежность** - ACID транзакции, репликация

Все компоненты приложения успешно интегрированы с Convex API и готовы к продакшн использованию.